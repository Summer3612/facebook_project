python3 --version

'''install and upgrade pip'''
python3 -m pip install --upgrade pip

'''Connect to EC2 remote mode from vs code'''
go to ssh configure file, compile user and ip. ensure that the key(*.pem) is executable by using chmod 400 user_key_pair.pem

'''install git on linux which has sudo yum'''
sudo yum install git